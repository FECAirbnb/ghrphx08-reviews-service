/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */
/* eslint-disable react/jsx-filename-extension */
import React from 'react';
import { shallow } from '../../enzyme';
import Popup from '../Popup';

describe('Popup component is rendered', () => {
  it('should render', () => {
    const testRender = <Popup />;
  });
});

// options: mockup Popup functionalities from the main App.jsx since it has all the functions
// somehow mock up all of the popup functionality from the component hmmm
